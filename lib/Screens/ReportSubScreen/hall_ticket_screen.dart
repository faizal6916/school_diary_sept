import 'package:flutter/material.dart';

class HallTicket extends StatelessWidget {
  const HallTicket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hall Tickets'),
    );
  }
}
